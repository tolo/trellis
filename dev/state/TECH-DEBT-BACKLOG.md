# Tech Debt Backlog — Trellis SDK

> Known technical debt. Items here are acknowledged but not yet prioritized for a milestone.

## Open Items

| ID | Area | Description | Severity | Discovered |
|----|------|-------------|----------|------------|
| TD-006 | themes / trellis_css / trellis_site | Dart Sass 3.0 forward-compat: theme SASS and the `_theme_params.scss` bridge use `@import` (deprecated, removed in Dart Sass 3.0.0) — warn-only today (Dart Sass 1.x), will not compile on Sass 3.0. **This is a bridge REDESIGN, not a mechanical find-replace** (assessed 2026-07-06): the site-first param override works by generating `_theme_params.scss` (`$trellis-*: <merged> !default;`) and `@import`-ing it *before* the theme's `_variables.scss` in one global scope, so bridge values win over the theme's `!default`s. `@use` isolates/namespaces module scope, so this load-order trick doesn't survive — migration means (a) switching the bridge to `@use '<theme-main>' with (<all params>)` module configuration in `theme_sass_generator.dart` + the CLI wrapper (`build_command.dart` `_compileSass`), (b) making every theme's `_variables.scss` `@use ... with()`-configurable and each partial `@use 'variables' as *` (Arbor partials carry 100s of `$trellis-*` refs; Verdant similarly), and (c) re-testing the param-override + skin contract for both themes. Scoped as its own effort; do not bundle with unrelated work. The deprecated `if()` builtin in `arbor/sass/_variables.scss` was already modernized to `@if`. **Spec'd 2026-08-17**: `docs/specs/sass-use-migration/` (private repo – `prd.md` + `fis-sass-use-migration.md`), with the `@use ... with()` design verified by a throwaway SCSS spike rather than assumed. Sequenced AFTER `docs/specs/site-redesign-themes/` (whose plan.json forbids touching the bridge) so the migration covers all six themes at once. Carries an open product decision (D2) the maintainer must settle before execution: today a forced skin defeats an explicit `theme_params:` colour override; under `@use` that precedence must be chosen deliberately. | Medium | docs-site |
| TD-011 | trellis_site | Theme SASS param values are not validated for structural CSS characters (`;`, `{`, `}`). `theme_sass_generator.dart` `_toSassValue` neutralizes SASS *interpolation* (`#{...}`, so it can't be evaluated — remediated in the 0.10.0 follow-up review), but a value carrying `;{}` still reaches the generated SCSS: a **string** value silently corrupts the output CSS (e.g. `content: a; } html { background: red;` closes the rule early and injects another), and a raw **color** value aborts the compile. **Scope — two sinks, values *and* names:** the same values also flow into the second emit sink `css/theme-props.css` (`_generateCssCustomProperties` / `_toCssValue`) — plain CSS copied verbatim to output with **no** compile gate, so corruption there is always silent (it never aborts). And it is not only param *values*: unknown site-override param **names** (`theme_params:` keys absent from the manifest) pass the merger warn-only and are emitted raw into **both** sinks, so a structural char in a key name has the same effect. A complete fix must cover both sinks and both values and names. Not a security boundary in the SSG model — params are build-time author config (theme.yaml / site `theme_params:`), not untrusted runtime input — so this is a robustness limitation (a typo produces broken output or a cryptic abort), not an exploit. Proper fix: a single value-validation/escaping policy across all param types (string/color/list/map) that either rejects structural chars with a clear `SiteConfigException` naming the param, or defines an explicit neutralization. Assessed as its own effort — do not bundle. Source: docs-site SASS remediation follow-up review 2026-07-07 (F-C residue). | Low | docs-site |
| TD-013 | trellis (FileSystemLoader) / trellis_dev | Dev-mode template watching does not see changes in **nested** template directories on **Linux**: `FileSystemLoader._startWatching` uses `Directory.watch(recursive: true)`, which dart:io documents as *not supported* on Linux (inotify) — only macOS (FSEvents) and Windows honour `recursive`. Surfaced by the first GitHub (ubuntu) run of `ci.yml` on 2026-08-18: `file_loader_test.dart` "nested subdirectory file change detected" times out deterministically there; the test is now `skip`ped on Linux (not a flake — a platform gap). User impact: hot reload (`trellis_dev`, `trellis serve`) on Linux dev boxes/containers misses edits under sub-folders of the template root. Fix: on Linux, walk the tree and watch each directory (and re-scan on directory create/delete), or use a watcher package. Note `ci.yml` is ubuntu-only, so nested-watch behaviour currently has no CI coverage on any OS — when fixing, either add a macOS runner for that test or turn the Linux skip into a negative assertion so the fix flips it loudly. Own effort; not bundled with 0.10.1. | Medium | 0.10.1 |
| TD-014 | trellis_site (feeds) | Date-only front matter (`date: 2026-01-01`, which is what the `trellis_cli` blog scaffold ships — `blog_content_templates.dart`) is parsed as *local* midnight, so `feed.xml` `<updated>` and `rss.xml` `pubDate` depend on the **build machine's timezone**: the same site yields `2025-12-31T23:00:00Z` on a CET laptop and `2026-01-01T00:00:00Z` on a UTC CI runner (`feed_generator.dart` `_resolveDateTime`; the sitemap `<lastmod>` uses the calendar date and is unaffected). Surfaced by the first ubuntu CI run 2026-08-18 (the `path_prefix` golden had baked CET); the test fixture now uses zone-explicit dates, the product behaviour is unchanged. Decide: treat date-only values as UTC (breaking for anyone relying on local), or document the rule and recommend zone-explicit dates in the scaffold. Small; own decision. | Low | 0.10.1 |
| TD-012 | trellis_shelf / trellis_dart_frog / trellis_relic | `htmxTrigger()` reads the `HX-Trigger` **request** header. HTMX 4 renames that request header to `HX-Source` and keeps `HX-Trigger` as a **response**-only header, so under HTMX 4 the function is both broken and misleadingly named. `htmxTarget()` is a milder case: the header survives but its value is reformatted to `tag#id`. Not actionable while Trellis tracks the HTMX `latest` dist-tag (2.x, per ADR-011) — renaming now would break the public API of three packages for no present benefit. Fix as part of the HTMX 4 migration when ADR-011's revisit trigger fires: rename to `htmxSource()`, repoint at `HX-Source`, document `htmxTarget()`'s new value format, and re-verify the OOB ordering assumptions in `renderOobFragments` (HTMX 4 reverses swap order and adds `<hx-partial>`). Source: HTMX 4 evaluation 2026-07-26 (ADR-011). | Low | 0.10.1 |

## Resolved

| ID | Package | Description | Severity | Origin | Resolution |
|----|---------|-------------|----------|--------|------------|
| TD-007 | trellis_site | `_resolvePrevNext` recomputed `orderedSectionPages` per rendered single page – O(k·n log n) per section | Low→**Medium** | docs-site | 2026-08-17 — **fixed**, and the benchmark showed it was worse than filed. `benchmark/site_scale_benchmark.dart` (new) drove it at 100→8000 pages: on an evenly-spread docs site the quadratic term is invisible (5.8 ms at 1000 pages), which is what made it look benign – but the term is *per section*, so a flat single-section blog is the worst case, and there it **breaches the 5 s build NFR at ~4000 posts** (1000: 403 ms · 2000: 1.85 s · 4000: 8.4 s · 8000: 39 s). Fixed by memoizing `orderedSectionPages` per `sectionPath` in a `_sectionOrderCache` scoped to one `generateAll` pass (cleared at pass start so a reused generator never serves stale order). Verified end to end: a full `generateAll` of a flat 2000-post blog – rendering and writing every page – now takes 351 ms, vs. 1.85 s for the prev/next resolution alone before. Two tests: neighbours match the unmemoized primitive across a whole 60-post section, and a reused generator recomputes on a changed page set (mutation-tested: fails without the `.clear()`). |
| TD-008 | trellis_site | `NavigationBuilder._buildLevel` rescanned the eligible page list at every recursion level (O(S×N)) | Low | docs-site | 2026-08-17 — **closed as not worth optimizing, on benchmark evidence.** `benchmark/site_scale_benchmark.dart` shows growth converging on 2× per 2× pages with ~10 µs/page flat from 500 to 8000 pages – i.e. effectively linear in practice, not the O(S×N) the filing feared, because S (section count) is small and fixed relative to N. Absolute cost: 10 ms at 1000 pages, 75 ms at 8000. Nowhere near the 5 s NFR at any plausible site size. The pre-grouping the entry proposed would add code for no measurable gain; reopen only if a benchmark on a deeply-nested tree (hundreds of sections) shows otherwise. |
| TD-004 | trellis (core) | `ProcessorContext.domProcessor` typed as `dynamic` to avoid circular import | Low | v0.3 | 2026-08-15 — resolved by extracting `FragmentHost`, a 6-member interface (`evaluator`, `processFragmentContent`, `querySelectorFromDoc`, `lookupFragment`, `push`/`popFragmentRegistry`) that `DomProcessor` implements; `domProcessor` is now typed to it and the `as DomProcessor` cast is gone. **The recorded reason was wrong**: the circular import is not a blocker — Dart permits import cycles, and typing the field against `processor.dart` directly analyzes clean (verified). The actual constraint was an API-boundary one, never written down: `ProcessorContext` is exported from `trellis.dart` while `DomProcessor` is not, so typing the field directly would put an unexported type on a public field. The narrow interface is exported instead, which also keeps engine internals out of the public surface. Verified: 1271 core tests green, `--fatal-infos` clean, `pub publish --dry-run` clean. |
| TD-005 | CI / publish | Publish workflow logs "Node.js 20 is deprecated" from `dart-lang/setup-dart` | Low | v0.8.2 | 2026-08-15 — **closed as stale.** Upstream migrated the action to Node 24 in `setup-dart` v1.7.2 (2026-03-23); v1.8.0 (2026-08-03) additionally fixed the Node pin inside the bundled reusable `publish.yml`. Every reference in this repo already resolves to a Node 24 build: `release-binaries.yml` pins SHA `65eb853c` (which is v1.7.2), and the `@v1` moving tag currently resolves to that same commit. The recorded cause therefore no longer exists. Caveat: the `v1` tag has **not** been advanced to v1.8.0, so `publish.yml@v1` misses v1.8.0's other changes. If a Node-deprecation warning still appears in a future run, it is coming from a different action in the job — check which action the log names before reopening. |
| TD-001 | trellis_cli | 56 info-level analyzer issues in test fixtures | Low | SDK Phase 1 | 2026-08-15 — **closed as no longer reproducible.** `dart analyze --fatal-infos` is clean across all 8 packages and the workspace root. The only analyzer exclude in play is `**/*.g.dart`, so the fixtures are genuinely being analyzed rather than skipped. Now guarded: CI runs `melos run analyze` (`--fatal-infos` per package) on every push, so a regression fails the build. |
| TD-010 | CI / release | trellis-public had no push/PR CI: nothing ran `dart analyze` or `dart test` before a release tag | Medium | binary-distribution | 2026-08-15 — resolved by `.github/workflows/ci.yml`. Two tiers: `check` (melos analyze + format:check + unit tests) on every push to any branch, and `e2e` (full suite) on main and via workflow_dispatch. Notes for future edits: melos scripts use `exec:`, which shells out to `melos` on PATH, so `dart run melos` is not sufficient — CI globally activates it; `melos run` prompts for package selection and crashes on a non-TTY, hence `--no-select`; and the e2e tier runs the full suite rather than `--tags=e2e`, because `dart test --tags=<tag>` exits 79 in packages where nothing carries the tag. Verified by running all four steps locally against the same commands. |
| TD-002 | trellis_site | 1 pre-existing failure in content_discovery_test | Medium | SDK Phase 2 | 2026-07-06 — root cause was the `empty_site/content` fixture dir, which git cannot track when empty (failed on fresh clones); the test now provisions the empty dir itself. Verified by deleting the dir and running the suite: 772/772 green. |
| TD-003 | trellis_cli (filed against trellis_site) | 2 pre-existing blog_e2e_test failures (concurrency) | Medium | SDK Phase 2 | 2026-08-15 — **closed as not reproducible.** `blog_e2e_test` starts no server; its only concurrency exposure was the process-global CWD mutation fixed under TD-009, which is the most likely original cause. Attempted repro: 3 concurrent `dart test test/blog_e2e_test.dart` runs, 12 consecutive full-suite runs, and 3 concurrent full e2e-tagged runs — zero failures. The original 2 failures were never characterized (no test names or output recorded), so this is closed on evidence of absence rather than an identified fix; reopen with captured output if it recurs. Note the item was filed against `trellis_site` but the test lives in `trellis_cli`. |
| TD-009 | trellis_cli | Test suite mutated process CWD and leaked scaffolded apps; package suite flaked under parallel `dart test` | Medium | SDK 0.9.0 | 2026-07-08 — root cause was process-global `Directory.current` mutation in the CLI test harness: concurrently-loaded suites raced on the shared CWD (passed only at `--concurrency=1`), corrupting even innocent readers (`version_test`, `blog_project_generator_test`) and leaking scaffolds into `packages/trellis_cli/` when a suite's tearDown left CWD at the repo root. Fixed by threading an injectable `workingDirectory` through `TrellisCli` into every command (create/build/serve/theme + the 5 theme subcommands), defaulting to `Directory.current` (zero user-facing change); each test injects a per-test `systemTemp` dir and no `Directory.current =` mutation remains. Scope exceeded the original create/theme note — `build_command_test`/`serve_command_test` shared the identical race, and `blog_e2e`'s chdir was removed too (does not address TD-003's separate blog_e2e concurrency failures). Verified: full `dart test packages/trellis_cli` at default concurrency 245/245 green (6/6 consecutive non-e2e runs deterministic, zero scaffold leaks); `dart analyze --fatal-infos` + `dart format` clean. |
