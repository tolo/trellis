---
title: Deploy
description: Publish the generated output with a static host.
weight: 30
---

Run `trellis build`, check links in `output/`, and publish that directory with
any static host. Set `baseUrl` and, for a project site, `pathPrefix` to match the
public URL before the production build.
