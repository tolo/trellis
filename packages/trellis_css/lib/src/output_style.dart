/// Controls the formatting of compiled CSS output.
enum OutputStyle {
  /// Human-readable output with newlines and indentation.
  expanded,

  /// Minified output with no unnecessary whitespace.
  compressed,
}
