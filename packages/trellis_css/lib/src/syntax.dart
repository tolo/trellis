/// The input syntax for SASS/SCSS source strings.
enum Syntax {
  /// Standard CSS-superset syntax with curly braces (`.scss` files).
  scss,

  /// Indented syntax without curly braces (`.sass` files).
  sass,
}
