name: trellis_css
version: 0.1.0
description: >-
  CSS utilities for the Trellis template engine — SASS compilation and
  fragment-scoped CSS via tl:scope.
repository: https://github.com/tolo/trellis/tree/main/packages/trellis_css
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  html: ^0.15.6
  sass: ^1.98.0
  trellis: ^0.7.0

dev_dependencies:
  lints: ^6.0.0
  path: ^1.9.0
  test: ^1.25.0
