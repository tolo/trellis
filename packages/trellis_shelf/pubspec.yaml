name: trellis_shelf
version: 0.9.1
description: >-
  Shelf integration for the Trellis template engine — middleware, HTMX helpers,
  and security defaults.
repository: https://github.com/tolo/trellis/tree/main/packages/trellis_shelf
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  crypto: ^3.0.0
  trellis: ^0.9.1
  shelf: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
