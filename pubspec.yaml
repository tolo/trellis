name: trellis
description: >-
  Template engine for Dart, using natural HTML templates. Fragment-first for
  hypermedia-driven frameworks like HTMX. Inspired by Thymeleaf.
version: 0.2.0
repository: https://github.com/tolo/trellis
issue_tracker: https://github.com/tolo/trellis/issues
topics:
  - template
  - html
  - htmx
  - server-side-rendering

environment:
  sdk: ^3.7.0

dependencies:
  html: ^0.15.6
  string_scanner: ^1.4.1

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
  coverage: ^1.15.0
